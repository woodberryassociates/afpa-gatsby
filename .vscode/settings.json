{
	// CSS/Styles
	"css.validate": false, // don't recall how the css is configured w/ tailwind, but it doesn't follow typical syntax
	// "languageStylus.useSeparator": false, // previously useful for CSS/tailwind decorator formatting
	// "postcss.validate": false, // previously useful for CSS/tailwind decorator formatting

	// Markdown
	// NOTE it would be nice to handle this on a per-file basis, instead of universally
	"markdownlint.config": {
		"MD041": false
	},

	// Spelling
	"cSpell.words": [
		"bbcec",
		"copays",
		"elasticlunr",
		"gform",
		"gtag",
		"icer",
		"WPCOM"
	]
}
